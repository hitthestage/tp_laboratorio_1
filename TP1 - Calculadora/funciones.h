#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED
#include <stdio.h>
#include <stdlib.h>

//Recibe valores de (x,y) y devuelve su suma
float sumar (float x, float y);
//Recibe valores de (x,y) y devuelve su resta
float restar(float x, float y);
//Recibe valores de (x,y) y devuelve su division
float dividir(float x, float y);
//Recibe valores de (x,y) y devuelve su multiplicación
float multiplicar(float x, float y);
//Recibe valores de (x) y devuelve su factorial
float factor (float x, float y);


#endif // FUNCIONES_H_INCLUDED
